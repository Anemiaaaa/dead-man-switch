.anchor
.DS_Store
target
**/*.rs.bk
node_modules
test-ledger
.yarn
.surfpool

# Trident fuzzing — build output and run artifacts. The generated sources
# (types.rs, fuzz_accounts.rs) and the hand-written test_fuzz.rs are tracked.
trident-tests/target
trident-tests/trident-artifacts
.vscode

# Go keeper — built binaries
keeper/bin/
keeper/keeper
keeper/dmsctl
keeper/.env

# Локальные ключи — никогда не коммитим
*-keypair.json
.env
